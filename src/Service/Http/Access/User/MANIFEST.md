# Access User HTTP Services

Grammar-aligned CRUD HTTP entrypoints for `access.user.*`.

`Index` and `Show` contain the existing business entrypoints. Remaining services are explicit skeletons until real CRUD business logic exists.
