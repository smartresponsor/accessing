# Access HTTP Services

Grammar-aligned CRUD and access-flow HTTP entrypoints for `access.*`.

`Index` and `Show` contain the existing business entrypoints. Remaining CRUD services are explicit skeletons until real CRUD business logic exists.

Canonical namespace:

```text
App\Accessing\Service\Http\Access
```

Canonical path:

```text
src/Service/Http/Access
```
